add <jem.jm>; // unused, please remove
out ('hello, world\n'); // prints hello world
// in (hello); function not yet implimented, still need variables first
return (0); // exits the code
// exit(0); exits the code
